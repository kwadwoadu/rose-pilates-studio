---
interface Props {
  image: string;
  alt: string;
}

const { image, alt } = Astro.props;
---

<section>
  <img
    src={image}
    alt={alt}
    class="w-full h-[40vh] md:h-[50vh] object-cover"
    loading="lazy"
  />
</section>
