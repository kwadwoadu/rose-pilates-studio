---
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section id="studio">
  <!-- Full-width image, no overlay -->
  <img
    src="/images/studio-interior.jpg"
    alt="Rose Pilates Studio interior"
    class="w-full h-[50vh] md:h-[60vh] object-cover"
    loading="lazy"
  />

  <!-- Text below -->
  <div class="section-padding">
    <div class="max-w-[720px] mx-auto scroll-animate">
      <span class="overline">{t('studio.label')}</span>
      <h2 class="mt-5 mb-10">{t('studio.heading')}</h2>
      <p class="leading-relaxed mb-8" style="color: var(--color-text-body);">
        {t('studio.text1')}
      </p>
      <p class="leading-relaxed" style="color: var(--color-text-body);">
        {t('studio.text2')}
      </p>
    </div>
  </div>
</section>
